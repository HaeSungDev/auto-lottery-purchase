import { Dayjs } from "dayjs"
import * as dayjs from "dayjs"
import { getUpcomingResultDay } from "../utils"
import { Page } from "puppeteer"

export const getBuyingLotteryCount = async (page: Page, resultDay: Dayjs = getUpcomingResultDay()) => {

}